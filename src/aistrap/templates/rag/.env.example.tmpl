# Copy this file to .env and adjust the values.
# .env is git-ignored, so your real secrets stay local.

APP_NAME={{project_name}}
LOG_LEVEL=INFO

# Retrieval settings
DOCUMENTS_DIR=documents
CHUNK_SIZE=400
CHUNK_OVERLAP=80
TOP_K=3

# Add an LLM provider key here when you plug one in, for example:
# OPENAI_API_KEY=
