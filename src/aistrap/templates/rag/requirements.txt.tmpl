# Runtime dependencies
# The starter retriever is pure standard library, so there is nothing heavy here.
python-dotenv>=1.0

# Development dependencies
pytest>=7.4

# Add your own stack when you are ready, for example:
# openai>=1.0
# chromadb>=0.5
# sentence-transformers>=3.0
