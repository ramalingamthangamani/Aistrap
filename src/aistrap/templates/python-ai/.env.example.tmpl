# Copy this file to .env and adjust the values.
# .env is git-ignored, so your real secrets stay local.

APP_NAME={{project_name}}
LOG_LEVEL=INFO

# Add the credentials your project needs, for example:
# OPENAI_API_KEY=
